# Data & cache (generated at runtime, at the repo root only).
# Anchored with a leading slash: an unanchored "data/" also matched the Android
# source package android/.../data/, silently keeping its whole data layer
# (api, repository, db) out of git.
/data/
/cache/
*.db

# Python virtual env
.venv/
venv/

# Backups (rotation dumps, pre-change snapshots)
backup/

# Build artifacts
*.apk

# Python
__pycache__/
*.pyc
*.pyo

# Logs (watchdog output, migration mapping files)
*.log
*.log.*
logs/

# Environment / secrets
# .env.bak* catches rotation backups, which still hold the previous secrets.
# .env.example stays tracked on purpose.
.env
.env.bak*
server.crt
server.key

# IDE
.vscode/
.idea/

# Claude Code
.claude/

# Internal agent/working instructions — not for the public repo.
/CLAUDE.md
# Internal audit/cleanup plan (references machine paths & security internals).
/docs/CLEANUP_PLAN.md

# SQLite WAL sidecar files at the repo root
/bookhaven.db-wal
/bookhaven.db-shm
*.db.bak*

# Public-domain demo instance (see docs/CLEANUP_PLAN.md §6)
/demo/

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Temp files
*.pid
